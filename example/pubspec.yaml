name: flutter_audio_streaming_example
description: Demonstrates how to use the flutter_audio_streaming plugin.


environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_audio_streaming:
    path: ../

  cupertino_icons: ^1.0.3
  wakelock: ^0.5.2
  path_provider: ^2.0.2
  audioplayers: ^0.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
